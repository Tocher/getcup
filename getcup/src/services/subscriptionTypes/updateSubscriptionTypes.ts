import {createService} from 'src/services/createService/createService';
import {updateStore} from 'src/services/common/updateStore';

export const updateSubscriptionTypes = createService(function updateSubscriptionTypes(state, data) {
    return updateStore.reducer({

    });
});
